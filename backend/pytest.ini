[pytest]
testpaths = tests
pythonpath = .
asyncio_default_fixture_loop_scope = function
markers =
    llm: optional real LLM/Embedding integration tests (require LLM_API_KEY)
filterwarnings =
    ignore:datetime.datetime.utcnow\(\) is deprecated:DeprecationWarning:jose.*
